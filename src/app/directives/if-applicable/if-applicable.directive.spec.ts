import { IfApplicableDirective } from './if-applicable.directive';

describe('IfApplicableDirective', () => {
  it('should create an instance', () => {
    const directive = new IfApplicableDirective();
    expect(directive).toBeTruthy();
  });
});
